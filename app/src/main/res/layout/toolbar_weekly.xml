<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.Toolbar xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:segmentedgroup="http://schemas.android.com/apk/res-auto"
    android:id="@+id/toolbar_weekly"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="20dp"
    android:minHeight="?attr/actionBarSize"
    app:popupTheme="@style/AppTheme.PopupOverlay">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="8dp"
        android:paddingLeft="6dp"
        android:paddingRight="24dp"
        android:paddingTop="8dp">

        <ImageButton
            android:id="@+id/weekly_return"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_centerVertical="true"
            android:background="@null"
            android:scaleType="centerInside"
            segmentedgroup:srcCompat="@drawable/return1"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="@dimen/weekly_top_bar_font_size"
            android:textColor="@color/colorWhite"
            android:text="周报"
            android:layout_centerHorizontal="true"/>

        <ImageButton
            android:visibility="gone"
            android:id="@+id/weekly_calendar"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:layout_marginLeft="24dp"
            android:layout_marginRight="12dp"
            android:adjustViewBounds="false"
            android:background="@null"
            android:scaleType="centerInside"
            segmentedgroup:srcCompat="@drawable/calendar_icon" />

    </RelativeLayout>
</android.support.v7.widget.Toolbar>